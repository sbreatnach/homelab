server {
    listen 80;
    listen [::]:80;

    server_name rspamd.{{ domains.internal }};

    location / {
        proxy_pass http://localhost:11334;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}