server {
    listen 80;
    listen [::]:80;

    server_name books.{{ domains.internal }};

    location / {
        proxy_pass http://localhost:8083;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}