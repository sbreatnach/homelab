server {
    listen 80;
    listen [::]:80;

    server_name localfm.{{ domains.internal }} localfm.{{ domains.vpn }};

    location / {
        proxy_pass http://localhost:8011;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}