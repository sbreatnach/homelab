server {
    listen 80;
    listen [::]:80;

    server_name photos.{{ domains.internal }} photos.{{ domains.vpn }};

    location / {
        proxy_pass http://localhost:4001;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}