server {
    listen 80;
    listen [::]:80;

    server_name music.{{ domains.internal }} music.{{ domains.vpn }};

    location / {
        proxy_pass http://localhost:4533;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
}