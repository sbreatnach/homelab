[Unit]
Description=Youtube Dowloader Web UI
After=remote-fs.target network.target
AssertPathExists={{ media.directories.ytdlpwebui }}

[Install]
WantedBy=multi-user.target

[Service]
User=ytdlpwebui
Type=simple
ExecStart={{ media.directories.ytdlpwebui }}/ytdlpwebui -conf {{ media.directories.ytdlpwebui }}/ytdlpwebui.yaml
WorkingDirectory={{ media.directories.ytdlpwebui }}
TimeoutStopSec=20
KillMode=process
Restart=on-failure
